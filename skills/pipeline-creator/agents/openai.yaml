interface:
  display_name: "Pipeline Creator"
  short_description: "生成基于 pipeline-framework 的业务流水线 skill"
  default_prompt: "Use $pipeline-creator to generate a runnable business pipeline skill based on pipeline-framework."
